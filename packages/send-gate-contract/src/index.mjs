// @soltrade/send-gate-contract — Send Gate CONTRACT + FAIL-CLOSED SKELETON (Gate E / E2-F-1).
// Contract/skeleton only: a fail-closed send gate that always refuses. No live mechanism, no RPC/provider,
// no send/broadcast, no transaction build/serialization, no network, no KMS, no KeyManager, no key material.
export * from './send-gate-contract.mjs';
