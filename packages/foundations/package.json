{
  "name": "@soltrade/foundations",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Build-Order foundation modules (deterministic, local): CostPipeline, CalibrationStore, RPCHealthMonitor, ProtocolConstantMonitor. No external calls; no trading; fail-safe.",
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "default": "./src/index.mjs"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --test test"
  }
}
