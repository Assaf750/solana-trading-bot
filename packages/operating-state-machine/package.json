{
  "name": "@soltrade/operating-state-machine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "OperatingStateMachine (Gate B / B10): deterministic EXITS_ONLY/KILLED safety wiring from health signals + action gating. In-memory; no live provider/network/DB; no execution authority.",
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "default": "./src/index.mjs"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --test test",
    "check:drift": "node ../../tools/check-ssot-drift.mjs"
  }
}
