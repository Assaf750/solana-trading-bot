// @soltrade/signing-adapter-contract — Signing Adapter CONTRACT + NO-OP adapter (Gate E / E2-C1).
// Contract/stub only: fail-closed signing adapter contract + no-op adapter. No crypto, no KMS/vault, no
// KeyManager, no key material, no signing/sending, no RPC/DB, no execution authority.
export * from './signing-adapter-contract.mjs';
