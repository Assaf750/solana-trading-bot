{
  "name": "@soltrade/intent-ledger",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "IntentLedger (Gate B / B2): deterministic in-memory intent records + idempotency + terminal-retention. No DB writes; no execution; no signing/sending.",
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "default": "./src/index.mjs"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --test test"
  }
}
