{
  "name": "soltrade-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Solana copy-trading engine — workspaces root. PR-A2 scope: ssot-types + contracts (SSOT-derived names only).",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test",
    "check:all": "node --test",
    "db:postgres:migrate": "node scripts/postgres-migrate.mjs",
    "smoke:postgres": "node scripts/smoke-postgres-storage.mjs",
    "smoke:redis": "node scripts/smoke-redis.mjs",
    "db:clickhouse:migrate": "node scripts/clickhouse-migrate.mjs",
    "smoke:clickhouse": "node scripts/smoke-clickhouse.mjs"
  },
  "dependencies": {
    "pg": "^8.13.0",
    "redis": "^4.7.0"
  }
}
