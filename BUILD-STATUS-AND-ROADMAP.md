# BUILD-STATUS-AND-ROADMAP.md — حالة البناء وخريطة الطريق إلى 100%

> **النوع:** وثيقة حالة/تخطيط حيّة غير سلطوية (living status + roadmap). تُقرأ بعد `CLAUDE.md` و`README.md`.
> **القاعدة الحاكمة:** عند أي تعارض مع `docs/00`–`12` / `CLAUDE.md` / `01-SSOT.md` تُغلَّب الوثيقة المعتمدة ويُصحَّح هذا الملف. لا تضيف أي اسم SSOT/API/CONFIG/DATA جديد. لا تُحوِّل `candidate_*` إلى implemented.
> **آخر تحديث:** 2026-06-11 · **الحالة على main:** `f8a8164`+ · full suite **1804/1804** · mechanism guard `sources=115 allowlist=1 violations=0` · SSOT drift baseline ثابت · `can_send:true`/`can_broadcast:true` غائبان repo-wide · ALLOWLIST = `Object.freeze(['packages/isolated-signer-runtime/src/'])` · **الأطوار A وB وC مكتملة + بوّاباتها PASS** · **الطور D جارٍ (Stage 19 Real Signing SIGN-ONLY ✅ بمراجعة أمنية مخصّصة + تصليبين · Stage 20 التالي)**.

---

## 0. ملخص تنفيذي
المشروع محرّك تداول كمّي على Solana لصيد العملات الجديدة ونسخ المحافظ الرابحة (wallet-led). تمّ بناء **العمود الفقري للأنبوب** `data → signal → risk → intent → route → sign → send` كسلسلة **حزم أساس read-only/advisory** نقيّة، fail-closed، بلا تنفيذ حيّ، بلا توقيع، بلا إرسال. كل مرحلة تستهلك مخرجات المرحلة السابقة وتنتج تمثيلاً وصفياً قابلاً للتدقيق فقط، ولا تفتح أي readiness/execution flag. الكود الحالي **سليم** (تدقيق متعدّد الوكلاء + حُرّاس آلية + drift + تدقيق مستقل = صفر عيوب). **الباقي إلى 100%** = إكمال طبقتي review للتوقيع والإرسال، ثمّ المحاكاة (paper) والمعايرة (calibration) — حيث تُقاس الربحية أوّل مرة بلا مال حقيقي — ثمّ البيانات الحيّة وواجهة المشغّل، ثمّ التوقيع الحقيقي والتنفيذ على testnet، وأخيراً تفعيل REAL-LIVE على mainnet خلف كل بوّابات الجاهزية/Hard-Risk/الأمان.

---

## 1. ما أُنجِز (DONE) — أساس الأنبوب read-only/advisory

| Stage | الحزمة / الإغلاق | exports | اختبارات الحزمة | commit الإغلاق |
|---|---|---|---|---|
| 2 | RPC Provider Foundations (`rpc-provider-contract`) — health/protocol-constant/spike boundaries | — | 257 | `08c3f28` |
| 3 | Gate-A Closure (`gate-a-foundations`) — config validation + audit path + readiness aggregation + status shell | — | 60 | `874a851` |
| 4 | Data Ingestion Foundation (`data-ingestion-foundations`) — source descriptor + replay/mock + normalized event + dedupe/cursor + health | — | 72 | `6d86d89` |
| 5 | Wallet/Token Intelligence Foundation (`wallet-token-intelligence-foundations`) — wallet/token obs + relationship + diagnostics + health | — | 63 | `8f5058a` |
| 6 | Signal Engine Foundation (`signal-engine-foundations`) — input boundary + wallet-led/token-activity candidate + scoring + suppression + health | 15 | 88 | `4aad905` |
| 7 | Risk Engine Foundation (`risk-engine-foundations`) — input boundary + hard-risk + liquidity/exit + exposure + verdict + suppression + health | 18 | 70 | `afcfd75` |
| 8 | Intent Ledger Foundation (`intent-ledger-foundations`) — input boundary + candidate record + in-memory ledger + state machine + audit envelope + suppression + health | 18 | 75 | `bf18d44` |
| 9 | Route / Execution-Planning Foundation (`route-planning-foundations`) — input/source boundary + candidate route + feasibility + execution-plan preview + suppression + health | 19 | 72 | `09c7212` |
| 10 | Transaction-Build-Review Foundation (`transaction-build-review-foundations`) — input/source boundary + candidate descriptor + resource advisory + **serialization-forbidden-surface guard (redacting)** + verdict + suppression + health | 20 | 75 | `5ba9631` |
| 11 | Signing-Review Foundation (`signing-review-foundations`) — input boundary + signer/custody boundary + candidate signing-review descriptor + custody-readiness advisory + **private-key-forbidden-surface guard (redacting NAME-only)** + verdict + **always-suppressed** suppression + health | 20 | 76 | `a466ed3` |
| 12 | Send/Broadcast-Review Foundation (`send-broadcast-review-foundations`) — send-review input boundary + sender/provider boundary (disabled tags) + candidate send-review descriptor + send-readiness advisory + **broadcast/live-forbidden-surface guard (redacting NAME-only)** + verdict + **always-suppressed** suppression + health · يتكامل مع `send-gate-contract` (يبقى يرفض) | 20 | 76 | `1eff85d` |
| 13 | End-to-End Decision-Trace Orchestrator (`pipeline-decision-trace-foundations`) — composer قراءة فقط فوق نتائج المراحل 6–12: **Decision Trace** واحد deterministic (6 مراحل، stage+state+decisive_reason فقط، **بلا copy-through** — أي حقل ممنوع داخل أي slot يحجب الـ trace كاملاً بصفر entries) + pipeline health read-model (worst-state-wins) + verdict + **always-suppressed** suppression + **pipeline forbidden-surface guard (redacting NAME-only)** + decision health | 14 | 34 | `5da1688` |
| **Gate A→B** | **Phase-A Gate PASS** — تكامل عبر-المراحل (سلسلة حقيقية 9 حزم → trace reviewed_advisory_all_stages) + regression كامل + تدقيق أمني (redaction على 4 حُرّاس · صفر سلطة تنفيذ) + 11 شرطاً مُلزِماً للطور B | — | — | `bef969c` |
| 14 | **Paper Execution Engine** (`paper-execution-foundations`) — **الطور B / أول قياس للربحية (simulated فقط)**: paper-fill descriptors (`simulated:true`/`is_valid_on_chain:false`) + **P&L read-model نقي FIFO** (realized FIFO · unrealized بشرط mark صالح · NET = gross − fees − slippage · تجميع per-wallet/mode/brain · oversell ignored · G22 candidate names فقط) + outcome classifier (حالات محلية) + suppression/surface-guard/health · **الرياضيات تحقّقت 4 مرات مستقلة بتطابق تام** · send-gate يبقى يرفض (لا paper bypass) | 14 | 29 | `e41fc00` |
| 15 | **Calibration & Backtest Harness** (`calibration-backtest-foundations`) — divergence read-model نقي simulated-vs-real (**finality = طابعا الزمن معاً** · bands من المستخدم لا defaults · متشائم عند صفر finalized · latency دائماً UNCLASSIFIED) + dataset descriptor **point-in-time/survivorship-free** (ترتيب `as_of_rank` · رفض مفاتيح future · المحافظ المنقرضة تبقى أو survivorship_risk) + **replay read-model حتمي TOCTOU-proof** (snapshot واحد يُتحقَّق ويُمشى · FIFO per-wallet + wins/losses) + suppression/guard/health · **المراجعة العدائية اكتشفت TOCTOU حقيقياً → DO_NOT_MERGE → إصلاح snapshot + اختباران انحداريان → إعادة مراجعة CLEAR** | 14 | 28 | `cf87dfc` |
| 16 | **Strategy & Wallet Profitability Intelligence** (`profitability-intelligence-foundations`) — read-model ربحية per-wallet (win_rate/win_loss_ratio · **profit_factor دائماً null لا يُختلق** · evidence ببارامتر المستخدم لا defaults) + أعلام مخاطر تشخيصية (thin-evidence/loss-dominant/one-hit · عتبات مفقودة = unevaluated لا clean) + **copyability advisory بفيتو fail-safe** (علم مخاطرة **يحجب net الموجب** · السقف KEEP_EVALUATING لا ترقية) · advisory_only بلا أي حقل أوامر · TOCTOU snapshot-once · `candidate_pnl_by_wallet` فقط من المسجّلة | 14 | 29 | `e0b39f9` |
| **Gate B→C** | **Phase-B Gate PASS** — سلسلة paper كاملة فوق نتائج حقيقية (fills→P&L→divergence/replay→intelligence→advisory بكل المخرجات الأربعة) + regression (1699/1699) + paper-truth/security (simulated دائماً · لا خلط · لا سلطة تنفيذ · فحص 163 تأكيداً من الحَكَم) + **11 شرطاً مُلزِماً للطور C** | — | — | `reports/E2-PHASE-B-GATE-EVIDENCE.md` |
| 17 | **Live Data Integration read-only / Live-Stream Boundary** (`live-stream-boundary-foundations`) — **الطور C**: مصادر حيّة كأوسمة disabled فقط + **activation-seam لا يجهز أبداً في هذه الحزمة** (`activation_performed:false` حرفية ثابتة · شرط مراجعة الـ adapter المنفصل hardcoded unmet) + stream-health/gap (سقفه EXITS_ONLY-shaped · لا KILLED) + readiness checklist (unknown=not-met) + الأسرار **بالمرجع فقط** (`provider_key_ref` بفحص شكل · raw keys مرفوضة + redacted) · لا network primitive في src · مسبار الحَكَم 164 ناتجاً بصفر فشل | 14 | 60 | `015c38b` |
| 18 | **Operator Dashboard read-only** (`operator-dashboard-foundations`) — 7 أسطح render نقية + مُجمِّع مستند **خامل** (لا form/button/script/fetch) فوق read-models: decision trace · pipeline health · paper P&L (**شارة SIMULATED إلزامية · نموذج بلا وسم يُرفض · null unrealized = unavailable لا 0 · بلا أي حساب مالي في الواجهة**) · profitability advisory · stream health (تحذير EXITS_ONLY مرئي لا يُخفى) · إشعارات أمنية **لا تُخفى أبداً** · XSS-escaped بالكامل · الأسرار تُرفض + redacted · AR/EN مع RTL/LTR · بطاريتا مسابير 52+71 بصفر فشل | 14 | 40 | `288b16d` |
| **Gate C→D** | **Phase-C Gate PASS** — تكامل render كامل فوق read-models حقيقية + regression (1799/1799) + امتثال الشروط الـ11 بنداً بنداً + **اكتساحات حيّة من الحَكَم** (السدّ لا يجهز بنيوياً عبر كل الأوسمة والتزوير · مفردات الـ advisory محصورة بالضبط في NONE/EXITS_ONLY_SHAPED) + **11 شرطاً مُلزِماً للطور D** (أهمها: مراجعة supply-chain قبل لمس signer-runtime · sign-only بلا مسار إرسال · مفاتيح لا تُحفظ ولا تُسلسل أبداً) | — | — | `reports/E2-PHASE-C-GATE-EVIDENCE.md` |
| 19 | **Real Signing (SIGN-ONLY)** (`isolated-signer-runtime`) — **الطور D**: WebCrypto Ed25519 sign-only (مبني سابقاً E2-C3) **مُحقَّق بمراجعة أمنية مخصّصة** (3 مراجعين + حَكَم · بطارية 28 no-key-leak · REVIEW_PASS صفر blockers مقابل 11 شرطاً) + **تصليبان**: كاشف key-material عميق (تعشيش/قيَم/حدود، over-refusal آمن) + audit fail-close (رمي append قبل/بعد يرفض بلا تسريب توقيع). مفاتيح ephemeral non-extractable لكل نداء · لا send · capabilities() كلها false · صفر تبعيات · ALLOWLIST = إدخال واحد دون تغيير | — | +5 تصليب (1804 إجمالي) | (hardening على main) |

**حُرّاس قائمة (Gate-A/H ومسبقة):** `send-gate-contract` (85)، `isolated-signer-runtime` (مُفعَّل في ALLOWLIST، skeleton/no-key)، إضافة لحزم Gate B–E السابقة (risk-gates · intent-ledger · position-lifecycle · execution-paper-adapter · decision-engine · exit-manager · paper-portfolio · execution-wallet-* · signer-* · custody-* · real-live-readiness) الموجودة كـ skeletons محوكمة.

**الثوابت المحفوظة في كل المراحل:** كل نتيجة `read_only:true` وكل أعلام readiness/execution = `false` (incl. `can_send`/`can_serialize`/`signing_permitted`/`transaction_ready`/`serialized_ready`/`message_bytes_ready`/`route_ready`/`order_ready`/`intent_ready`/`risk_ready`/`signal_ready`/`live_quote_enabled`/`mainnet_enabled`/`real_live`). كل حزمة import-free، deterministic، fail-closed، بلا module-level mutable state، بلا network/clock/persistence/secret؛ كل eligibility تشترط الحالة النهائية الصحيحة للمرحلة السابقة؛ الأسرار/المسارات/artifacts التسلسل لا تُكرَّر أبداً (redaction). **لا SSOT name جديد · ALLOWLIST ثابت · drift baseline ثابت.**

> **ما لم يبدأ بعد (مغلق صراحةً):** أي بناء transaction حقيقي · serialization/message-bytes/signature · توقيع حقيقي · send/broadcast حقيقي · Jupiter live/live quote/aggregator · بيانات حيّة فعلية (Helius LaserStream / Triton-Yellowstone) · paper execution مُشغَّل · mainnet · REAL-LIVE.

---

## 2. نموذج العمل لكل مرحلة (Definition of Done — مراجعة واختبارات بعد كل إنجاز)
كل Stage يُنفَّذ بالدورة الثابتة التالية، ولا يُدمج جزء إلا وهو **PASS بلا blockers**، **fast-forward only**، `branch = main + 1`، parent مطابق لـ main:
1. **Inventory** مختصر (ملفات · package · أسماء SSOT؟ · dependencies؟ · guard delta).
2. **Multi-agent build workflow** (≥5 أدوار: implementation · build-test · security-guard · governance/SSOT · behavioral + arbiter) — implementation-first (code+tests).
3. **Main-loop verification** مستقلّة + **behavioral spot-check** على مدخلات حقيقية من المرحلة السابقة.
4. **Evidence report** لكل PR داخلي (`reports/E2-...`).
5. **Separate multi-agent pre-merge workflow** يعيد التحقّق ويُرجِع `CLEAR_TO_MERGE` بصفر blockers.
6. **ff-merge** ثمّ **post-merge verification**.
7. **Stage closure report** + **Stage Final Report** ثمّ **STOP** (لا مرحلة تالية بلا أمر صريح).

**بوّابة على مستوى الطور (Phase Gate):** بعد إنهاء طور كامل (A–E أدناه) تُجرى **مراجعة تكامل عبر-المراحل + regression كامل + توقيع حوكمي/أمني** قبل فتح الطور التالي. الحُرّاس الإلزامية الخضراء في كل دمج: `check-ssot-drift` (baseline ثابت) · `check-mechanism-guards` (`allowlist=1 violations=0`) · full `node --test` · `can_send:true` غائب repo-wide · ALLOWLIST غير معدّل · scope = الملفات المسموحة فقط.

---

## 3. خريطة الطريق إلى 100% — الأطوار والمراحل المتبقّية

> كل مرحلة أدناه تتبع نموذج §2. التعيين إلى Gates A–E من `10-AGENT-BUILD-PLAN §7` و`06-BUILD §6` (Safety Activation Gates). **لا يُبدأ أي طور تالٍ بلا أمر مستخدم صريح ومنفصل.**

### الطور A — إكمال أنبوب المراجعة read-only/advisory (بلا تنفيذ) — *Gate A/B*
- **Stage 11 — Signing-Review Foundation — ✅ DONE (`a466ed3`):** مراجعة جاهزية التوقيع وصفياً فقط (signer-profile/custody-readiness descriptors من metadata · `key_custody_mode` advisory · dual-control review · `signer_profile_status` review) + **Private-Key Forbidden Surface Guard** (يحجب أي اسم مفتاح/seed/keypair/signature ويُرجِع `forbidden_field_ref` = الاسم فقط، والقيمة المزروعة غائبة عن `JSON.stringify`) + verdict + **suppression دائمة الكتم لـ sign/send** (`not_sign/send/execution_authorized` على كل مسار) + health. يستهلك `TX_BUILD_REVIEW_PASS_ADVISORY`. **بلا أي private key material · بلا توقيع حقيقي · بلا تفعيل SignerService.** كل أعلام readiness/signing = false على كل حالة (incl. PASS_ADVISORY/ACCEPTABLE_ADVISORY/REVIEWED_ADVISORY). الحزمة: `signing-review-foundations` · 20 export · 76 اختبار. إغلاق: `reports/E2-STAGE-11-...-CLOSURE-EVIDENCE.md`.
- **Stage 12 — Send/Broadcast-Review Foundation — ✅ DONE (`1eff85d`):** مراجعة شروط الإرسال وصفياً فقط (sender/provider descriptors disabled/read-only · bundle/tip advisory buckets · idempotency/intent review) + **Broadcast/Live Forbidden Surface Guard** (يحجب أي اسم endpoint/url/serialized-tx/signed-tx/signature ويُرجِع `forbidden_field_ref` = الاسم فقط، والقيمة المزروعة غائبة عن `JSON.stringify`) + verdict + **suppression دائمة الكتم** (`not_send/broadcast/execution_authorized`) + health. يستهلك `SIGNING_REVIEW_PASS_ADVISORY`. يتكامل مع `send-gate-contract` القائم (يبقى يرفض `ok:false`). **بلا send/broadcast حقيقي · `can_send`/`can_broadcast`/`broadcast_permitted` تبقى false على كل حالة.** الحزمة: `send-broadcast-review-foundations` · 20 export · 76 اختبار. إغلاق: `reports/E2-STAGE-12-...-CLOSURE-EVIDENCE.md`.
- **Stage 13 — End-to-End Decision-Trace Orchestrator (read-only) — ✅ DONE (`5da1688`):** تركيب المراحل 4–12 في **Decision Trace** واحد deterministic + full-pipeline health/status read-model، يُظهر سبب القبول/الرفض لكل قرار عبر الأنبوب. لا تنفيذ — composer نقي فوق نتائج المراحل؛ **بلا copy-through** لأي endpoint/serialized/signature/key؛ كل أعلام التنفيذ false على كل حالة. الحزمة: `pipeline-decision-trace-foundations` · 14 export · 34 اختبار. إغلاق: `reports/E2-STAGE-13-...-CLOSURE-EVIDENCE.md`.

**✅ الطور A مكتمل.** **Phase-A Gate** (مراجعة تكامل عبر-المراحل + regression كامل + توقيع حوكمي/أمني عبر workflow متعدّد الوكلاء) يُنفَّذ قبل فتح الطور B — نتيجته تُوثَّق في `reports/E2-PHASE-A-GATE-INTEGRATION-REGRESSION-EVIDENCE.md`.

### الطور B — المحاكاة والمعايرة (قابل للتنفيذ، بلا مال حقيقي — هنا تُقاس الربحية أوّل مرة) — *Gate B*
- **Stage 14 — Paper Execution Engine — ✅ DONE (`e41fc00`):** حزمة `paper-execution-foundations` (A–G): boundary لا يقبل إلا `PIPELINE_DECISION_REVIEWED_ADVISORY` · paper-fill descriptors محاكاة دائماً · **P&L read-model نقي FIFO** (realized/unrealized بشرط mark صالح/fees/slippage/NET/تجميع wallet/mode/brain، G22 فقط) · outcome محلي · suppression دائمة + surface guard + health. الرياضيات تحقّقت 4 مرات مستقلة. send-gate يبقى يرفض. **بلا live · بلا signer · بلا تنفيذ حقيقي.** إغلاق: `reports/E2-STAGE-14-...-CLOSURE-EVIDENCE.md`.
- **Stage 15 — Calibration & Backtest Harness — ✅ DONE (`cf87dfc`):** حزمة `calibration-backtest-foundations` (A–G): divergence simulated-vs-real بقاعدة finality (طابعا الزمن معاً، متطابقة مع `CalibrationStore` القائم) · dataset descriptor point-in-time/survivorship-free · replay حتمي TOCTOU-proof · suppression/guard/health. **بلا future leakage · المحافظ المنقرضة ضمن العيّنة · bands من المستخدم فقط.** المراجعة العدائية أمسكت TOCTOU حقيقياً قبل الدمج (أُصلح + انحداريات + إعادة مراجعة CLEAR). إغلاق: `reports/E2-STAGE-15-...-CLOSURE-EVIDENCE.md`.
- **Stage 16 — Strategy & Wallet Profitability Intelligence — ✅ DONE (`e0b39f9`):** حزمة `profitability-intelligence-foundations` (A–G): ربحية per-wallet + أعلام مخاطر heuristic + copyability advisory بفيتو (علم مخاطرة يحجب الربح الظاهري · السقف KEEP_EVALUATING). **advisory فقط · لا auto-apply · لا P&L على Opportunity/Radar · الأسماء المؤجَّلة في SSOT بقيت مؤجَّلة (حقول محلية `profitability_*`).** إغلاق: `reports/E2-STAGE-16-...-CLOSURE-EVIDENCE.md`. **Phase-B Gate (التالي):** مراجعة تكامل 14–16 + regression + إثبات انضباط paper-truth (simulated دائماً · paper ≠ live) — نتيجتها في `reports/E2-PHASE-B-GATE-EVIDENCE.md`.

### الطور C — البيانات الحيّة (read-only) + واجهة المشغّل — *Gate B→C*
- **Stage 17 — Live Data Integration (read-only) — ✅ DONE (`015c38b`):** حزمة `live-stream-boundary-foundations` (A–G): boundary مصادر حيّة disabled-by-default + activation-seam وصفي لا يفعّل أبداً + stream-health/gap read-model (سقف EXITS_ONLY) + readiness checklist. **الأسرار بالمرجع فقط · لا اتصال حيّ في أي مكان — الاتصال الفعلي يحتاج مفاتيح API من المالك في حزمة adapter منفصلة بمراجعتها الخاصة.** إغلاق: `reports/E2-STAGE-17-...-CLOSURE-EVIDENCE.md`.
- **Stage 18 — Operator Dashboard / UI — ✅ DONE (`288b16d`):** حزمة `operator-dashboard-foundations`: طبقة العرض read-only فوق كل الـ read-models القائمة (7 أسطح + مُجمِّع خامل، AR/EN، RTL/LTR، unavailable لا يُختلق، SIMULATED موسوم دائماً، الإشعارات الأمنية لا تُخفى، XSS-safe، أسرار مرفوضة). **صفحات الأوامر التشغيلية الكاملة من `11-UI-SPEC` (Flows B–E وCatalog A) تتطلب طبقة API الحقيقية وتُبنى مع مراحلها اللاحقة.** إغلاق: `reports/E2-STAGE-18-...-CLOSURE-EVIDENCE.md`. **Phase-C Gate (التالي):** مراجعة تكامل 17–18 + regression + امتثال شروط الطور C — نتيجتها في `reports/E2-PHASE-C-GATE-EVIDENCE.md`.

### الطور D — التوقيع الحقيقي والتنفيذ على testnet (أمان Gate C/D) — *Gate C/D*

**✅ الطوران B وC مكتملان + بوّابتاهما PASS** (`reports/E2-PHASE-B-GATE-EVIDENCE.md` · `reports/E2-PHASE-C-GATE-EVIDENCE.md`، 11 شرطاً مُلزِماً للطور D).

- **Stage 19 — Real Signing Implementation (SIGN-ONLY) — ✅ DONE (verification + hardening):** WebCrypto Ed25519 sign-only داخل `isolated-signer-runtime` (الإدخال الوحيد في ALLOWLIST) كان مبنياً من سلسلة E2-C3؛ Stage 19 = **مراجعة supply-chain/أمنية مخصّصة** (3 مراجعين + حَكَم، بطارية 28 فحصاً no-key-leak) قابلتها الشروط الـ11 → **REVIEW_PASS صفر blockers** + تنفيذ شرطَي تصليب مُلزِمين: (1) كاشف key-material **عميق** (تعشيش + قيَم PEM/base58/mnemonic، حدود depth/budget، over-refusal آمن) · (2) **audit fail-close** (رمي append قبل/بعد التوقيع يرفض ولا يُسرّب توقيعاً غير مُدقَّق). مفاتيح ephemeral non-extractable مُعطاة لكل نداء · لا send · capabilities() كلها false · صفر تبعيات (node:crypto فقط). إغلاق: `reports/E2-STAGE-19-...-EVIDENCE.md`. (1804/1804، +5 اختبارات تصليب.)
- **Stage 20 — Key Management / Custody / Execution-Wallet Lifecycle:** KMS/Vault adapter (بعد supply-chain review منفصل) · execution-wallet admission/drain/revoke/rotation/sweep · `signer_control` dual-control · break-glass + audit retention. **`signer_control` صلاحية منفصلة · لا raw key في UI/DB/logs/exports.**
- **Stage 21 — Testnet/Devnet Execution:** إرسال حقيقي على testnet **فقط** (Helius Sender / Jito send/bundle خلف `send-gate-contract`) · IntentLedger idempotency · FailedTransactionClassifier · bundle status observer. **mainnet ممنوع · REAL-LIVE ممنوع.** **Phase-D Gate:** مراجعة أمنية كاملة + isolation/no-key-leak + regression + توقيع `signer_control`.

### الطور E — تفعيل REAL-LIVE على mainnet (Gate E — قرار المستخدم النهائي) — *Gate E*
- **Stage 22 — REAL-LIVE Readiness + Hard-Risk Wiring:** ربط `real_live_config_valid` + Hard-Risk limits (daily-loss/drawdown/exposure kills) + Calibration Kill/Pause + Readiness Checklist (priority-fee/Jito-tip cache · protocol constants · RPC green · stream sync · calibration priors · cost pipeline) + `activate_real_live` command path. **config غير صالح لـ REAL-LIVE إن غابت حدود Hard Risk.**
- **Stage 23 — Mainnet REAL-LIVE Activation:** التفعيل النهائي خلف كل البوّابات بقرار مستخدم صريح، برأس مال محدود ومراقَب + global kill switch + Emergency Exit. **يبقى Fail-Safe-Not-Fail-Open.** **Phase-E Gate:** توقيع جاهزية نهائي + خطة rollback/incident.

---

## 4. تسلسل البناء الإلزامي (مرجع — من `CLAUDE.md`/`06-BUILD §4`)
`CostPipeline → CalibrationStore → RPCHealthMonitor → ProtocolConstantMonitor → SignerService/KeyManager → PositionLifecycleStateMachine → IntentLedger`. وحدات live hardening وSignerService/KeyManager **إلزامية قبل REAL-LIVE**. الأطوار أعلاه تحترم هذا الترتيب: المعايرة (B) قبل الحيّ (C)، والتوقيع المعزول (D) قبل التفعيل (E).

## 5. مخاطر/قرارات حوكمية مفتوحة قبل الأطوار اللاحقة
- **أي اسم جديد** يلزم لأي مرحلة (مثل signing-review/send-review states) يبقى **function-I/O local فقط** (سابقة معتمدة) أو يمرّ `ARCH → SSOT` قبل الاستخدام — لا اسم خارج SSOT.
- **dependency جديدة** (signing lib · KMS SDK · chart lib · stream client) تتطلّب **تقرير supply-chain منفصل** قبل الإضافة؛ والأفضل تأجيلها لأبعد طور ممكن.
- **REAL-LIVE / send / mainnet / private key** تبقى مغلقة بشروط `06-BUILD §6` ولا تُفتح إلا بأمر مستخدم صريح لكل طور.

## 6. الخلاصة
الكود الحالي سليم وكل الحُرّاس خضراء. الطريق إلى 100% واضح ومتدرّج: **مراجعة → محاكاة/معايرة (قياس الربح) → بيانات حيّة + واجهة → توقيع معزول + testnet → REAL-LIVE**. الترتيب يضع **قياس الربحية والأمان قبل المال الحقيقي**، وهو الأذكى لمحرّك يستهدف الربح. يبدأ كل طور بأمر مستخدم صريح ومنفصل، وينتهي بمراجعة واختبارات وبوّابة حوكمية.
